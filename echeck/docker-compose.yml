version: '2'
services:
  base:
    restart: unless-stopped
    build: .

  app:
    restart: unless-stopped
    extends: base
    ports:
      - '3000'


  crond:
    restart: unless-stopped
    extends: base
    command: enterpoint.sh crond

# 添加网卡
# 添加需要的环境变量
# 添加base项目映射的端口
